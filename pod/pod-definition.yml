apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  containers:
    - name: nginx-container
      image: nginx:1.21.6
      imagePullPolicy: IfNotPresent
  restartPolicy: Always